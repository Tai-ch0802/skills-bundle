---
name: xlsx
description: "只要任何具有將試算表檔案作為其主要輸入或輸出的場景，請使用此技能。這表示任何當使用者想要有關於執行這項任務，如將現有的這 .xlsx, .xlsm, .csv，或是這 .tsv 的檔案打開以及去對這些檔案進行讀取或是修改與編輯、修復等的時候皆應如此來運用（諸如像是對於它加入了資料行、計算公式、排版與美編格式化、建立圖表，和清理雜亂無章的資料）。當使用者提及試算表（如「在我的下載裡的 xlsx」）及想建立新試算表時，或欲做檔案格式轉換、處理亂碼或亂成一團的行列表頭以導正回正常的表格等。請勿在主產出成果為 Word 文件, HTML 報表, 或一個自有的獨立 Python 腳本、資料庫管線，或是 Google Sheets API 整合的情況下來運用，即使其中涉及了表格資料處理，只要產出物不是試算表就不該觸發。"
license: Proprietary. 完整條款請見 LICENSE.txt
---

# 輸出要求

## 所有 Excel 檔案

### 專業字型
- 除非使用者另有指示，否則所有交付成果皆應使用一致且專業的字型（例如 Arial、Times New Roman）。

### 零公式錯誤
- 每個 Excel 模型在交付時必須保證**零**公式錯誤 (#REF!, #DIV/0!, #VALUE!, #N/A, #NAME?)。

### 保留現有範本（更新範本時）
- 修改檔案時，務必研究並**完全符合**其現有格式、樣式與慣例。
- 絕對不要將標準化格式強加到已有既定模式的檔案上。
- 現有範本的慣例**永遠**優先於這些指南。

## 財務模型

### 顏色編碼標準
除非使用者或現有範本另有說明：

#### 業界標準的顏色慣例
- **藍色文字 (RGB: 0,0,255)**：寫死的輸入值（Hardcoded inputs），以及使用者為了情境而將更改的數字。
- **黑色文字 (RGB: 0,0,0)**：所有的公式和計算。
- **綠色文字 (RGB: 0,128,0)**：從相同活頁簿內的其他工作表提取資料的連結。
- **紅色文字 (RGB: 255,0,0)**：連結至其他檔案的外部連結。
- **黃色背景 (RGB: 255,255,0)**：需要注意的關鍵假設，或需要被更新的儲存格。

### 數字格式化標準

#### 必須遵守的格式規則
- **年份**：格式化為文字字串（例如 "2024" 而非 "2,024"）。
- **貨幣**：使用 $#,##0 格式；務必在標題中指定單位（如 "營收 ($mm)"）。
- **零值 (Zeros)**：使用數字格式使所有零顯示為 "-"，包括百分比（例如 "$#,##0;($#,##0);-"）。
- **百分比**：預設為 0.0% 格式（小數點後一位）。
- **乘數/倍數 (Multiples)**：為估值倍數（EV/EBITDA, P/E）格式化為 0.0x。
- **負數**：使用括號 (123) 而不是減號 -123。

### 公式建構規則

#### 假設位置
- 將所有假設（增長率、利潤率、倍數等）放在獨立的假設儲存格中。
- 在公式中使用儲存格參照，而不是寫死的數值。
- 範例：使用 `=B5*(1+$B$6)` 而不是 `=B5*1.05`

#### 防止公式錯誤
- 驗證所有儲存格參照是否正確。
- 檢查範圍是否發生差一錯誤 (off-by-one errors)。
- 確保所有預測期間的公式保持一致。
- 使用邊緣情況（零值、負數）進行測試。
- 驗證沒有意外的循環參照。

#### 寫死數值的文件要求
- 在儲存格內加上註解或在旁邊儲存格說明（如果在表格末端）。格式："來源: [系統/文件], [日期], [具體參考], [如果有 URL 則附上]"
- 範例：
  - "來源: 公司 10-K, FY2024, 頁 45, 營收備註, [SEC EDGAR URL]"
  - "來源: 公司 10-Q, Q2 2025, 附件 99.1, [SEC EDGAR URL]"
  - "來源: 彭博終端機, 8/15/2025, AAPL US Equity"
  - "來源: FactSet, 8/20/2025, 預估共識篩選"

# XLSX 建立、編輯與分析

## 概覽

使用者可能會要求你建立、編輯或分析 .xlsx 檔案的內容。針對不同的任務，你有不同的工具和工作流程可供使用。

## 重要要求

**重新計算公式需要 LibreOffice**：你可以假設已安裝 LibreOffice，可使用 `scripts/recalc.py` 腳本來重新計算公式值。該腳本會在首次執行時自動配置 LibreOffice，包括在 Unix sockets 受到限制的沙盒環境中（由 `scripts/office/soffice.py` 處理）。

## 讀取和分析資料

### 使用 pandas 進行資料分析
對於資料分析、視覺化和基本操作，請使用提供了強大資料操作功能的 **pandas**：

```python
import pandas as pd

# 讀取 Excel
df = pd.read_excel('file.xlsx')  # 預設：第一個工作表
all_sheets = pd.read_excel('file.xlsx', sheet_name=None)  # 所有工作表以 dict 呈現

# 分析
df.head()      # 預覽資料
df.info()      # 欄位資訊
df.describe()  # 統計數據

# 寫入 Excel
df.to_excel('output.xlsx', index=False)
```

## Excel 檔案工作流程

## 關鍵 (CRITICAL)：使用公式，而非寫死數值

**始終使用 Excel 公式，而不是在 Python 中計算值並將其寫死。** 這確保試算表保持動態並可更新。

### ❌ 錯誤做法 - 將計算後的值寫死
```python
# 不良示範：在 Python 中計算並將結果寫死
total = df['Sales'].sum()
sheet['B10'] = total  # 寫死了 5000

# 不良示範：在 Python 中計算成長率
growth = (df.iloc[-1]['Revenue'] - df.iloc[0]['Revenue']) / df.iloc[0]['Revenue']
sheet['C5'] = growth  # 寫死了 0.15

# 不良示範：在 Python 計算平均數
avg = sum(values) / len(values)
sheet['D20'] = avg  # 寫死了 42.5
```

### ✅ 正確做法 - 使用 Excel 公式
```python
# 良好示範：讓 Excel 去計算總和
sheet['B10'] = '=SUM(B2:B9)'

# 良好示範：成長率作為 Excel 公式
sheet['C5'] = '=(C4-C2)/C2'

# 良好示範：使用 Excel 公式計算平均數
sheet['D20'] = '=AVERAGE(D2:D19)'
```

這適用於「所有」計算 - 總計、百分比、比率、差值等。當來源資料改變時，試算表必須能夠進行重新計算。

## 常見工作流程
1. **選擇工具**：用 pandas 處理資料，用 openpyxl 處理公式/格式
2. **建立/載入**：建立新活頁簿或載入現有檔案
3. **修改**：新增/編輯資料、公式和格式
4. **儲存**：寫入檔案
5. **重新計算公式（如果使用了公式則為強制性要求）**：使用 scripts/recalc.py 腳本
   ```bash
   python scripts/recalc.py output.xlsx
   ```
6. **驗證並修復任何錯誤**：
   - 腳本會返回帶有錯誤詳細資訊的 JSON
   - 如果 `status` 為 `errors_found`，請檢查 `error_summary` 以瞭解特定的錯誤類型與位置
   - 修復識別出的錯誤，然後再次重新計算
   - 常見的需修復錯誤：
     - `#REF!`: 無效的儲存格參照
     - `#DIV/0!`: 除以零
     - `#VALUE!`: 公式中的資料類型錯誤
     - `#NAME?`: 無法識別的公式名稱

### 建立新的 Excel 檔案

```python
# 使用 openpyxl 來設定公式與格式
from openpyxl import Workbook
from openpyxl.styles import Font, PatternFill, Alignment

wb = Workbook()
sheet = wb.active

# 加上資料
sheet['A1'] = 'Hello'
sheet['B1'] = 'World'
sheet.append(['Row', 'of', 'data'])

# 加上公式
sheet['B2'] = '=SUM(A1:A10)'

# 樣式格式
sheet['A1'].font = Font(bold=True, color='FF0000')
sheet['A1'].fill = PatternFill('solid', start_color='FFFF00')
sheet['A1'].alignment = Alignment(horizontal='center')

# 欄位寬度
sheet.column_dimensions['A'].width = 20

wb.save('output.xlsx')
```

### 編輯已有的 Excel 檔案

```python
# 運用 openpyxl 在保有公式和已經被賦予的格式樣貌上
from openpyxl import load_workbook

# 取出現有的案檔
wb = load_workbook('existing.xlsx')
sheet = wb.active  # 或著是用 wb['SheetName'] 開那指定的分頁

# 當要操作對象擁有多分頁的狀況
for sheet_name in wb.sheetnames:
    sheet = wb[sheet_name]
    print(f"Sheet: {sheet_name}")

# 改那特定儲存格
sheet['A1'] = 'New Value'
sheet.insert_rows(2)  # 在列2的位子做加上行
sheet.delete_cols(3)  # 將直的第 3 列砍除 

# 在這個簿內中再生出一新的表頁
new_sheet = wb.create_sheet('NewSheet')
new_sheet['A1'] = 'Data'

wb.save('modified.xlsx')
```

## 關於公式更新與重計的處置

藉由了 openpyxl 加建入以及其變過之等檔中所具此那它裡這個之公式乃作為等有如成串等的文符，但尚那其中未被運和算是被作估它產生的值。必須使並有等去用予這給出及等它有之 `scripts/recalc.py` 此這指令將去和等對於等與及算等那其：

```bash
python scripts/recalc.py <excel_file> [timeout_seconds]
```

如下舉一例之 : 
```bash
python scripts/recalc.py output.xlsx 30
```

有這個的腳等這程本和：
- 這會等於其第在它首次使用上跑自動為及與之設立好它此之巨集的給 LibreOffice 
- 這裡去及那等於為其全簿的分等頁中其之它所有公皆去更新這 
- 找在等全那之格子裡面與它是與及有這錯它這及 (#REF!, #DIV/0!, etc.) 之處為去搜查
- 提供之這給將所帶會附上所有在有等細等誤之其位這置的等之數此那以以 JSON 將其傳之。 
- macOS 還是其 Linux 都能在有跟這和跑之上 

## 這個有它是要與檢查及其公式它用等的核和確之等之清用此單之

這裡之快給去能是它是於讓等驗在有確保它公式它其與無跑順等有之這:

### 這等那基本去必須等它要去確這能是核這
- [ ] **試這個等它那2-3等範及去等的其。這給等這其去給參考有這這等**：為這確建及在等之滿與那全模並造前驗這看有沒有抽和對。
- [ ] **有這及去要等那於此等對這此及行它的它。這這**：為這去確於 Excel 是這及和等行有它這有合去 (及其如, 去之 64 等 = BL, 而不這是與等 BK)
- [ ] **這及那個去有對於等那在其列等之和這這之有。位移它這（Row offset）這**：及請記與以它 Excel 那它等。是從 1開始和有基這及之（於有在這的 DataFrame 這行及列等有這 5 將應 = 它 Excel 那這是等其列 6）

### 它這是常這等及有它與這等在有這。去遇有為見犯這的等這之此的它這。之 
- [ ] **對此與那去這及 NaN 它的會是有此這等處。理這**：這這，以那等這個。去之 `pd.notna()` 等這把有等之這是與這這為這等這給測及檢有無。空 
- [ ] **在及這最。這等右等其邊的那它等之直。這此與列這**：等它的及對於 FY 它在這之常常這是在與。這這有等這。列有之大等 50+
- [ ] **其和於去能等那在有多等和之筆有和相它。等這等之。匹等。那符这合有**：去找出。這有全它這有等。等這與等發生這和所有的，與而不要去。等那這這第一個這它。有。
- [ ] **其等及會。有有它等之為等零以這。有。之去這和其除以。這有被它。去**：在它要用到那這個這等與。在去及等這其那有公式 `/` 的之。前，先。它要確它去對其去分有及母它為等這。有檢等 (#DIV/0!)。
- [ ] **它其在去這為那引用此有和錯誤這去。**：要這確它與定有在及等那所這有的這個其等等有去將等它儲及有存等之能格子有那去等它和是有為指去到了真。這的它那正和向。有之及所它等那意其去對等之這 (#REF!)這。
- [ ] **有等其去之為及有去在此其不與同之等這這等那和跨在這各個表及等地它這互相等等引用**：及與用等這個在這去正它及的這用為在那等式和及格。(這有這有及這這其 Sheet1!A1)，那及以那給這之。和有為等這和與它來有互連等。及結等。這有這及表 

### 測及測量等及與它試這它及。等。在其這個之之略與測
- [ ] **其和這這這個有在它和此起於小處這有。及等和去開始**：及對為在用於把。和它及全面有全套此並擴廣等之給以於前先對有這去等能它把在 2-3 有這與有個格和去中有先用。去公式它和測試去它這 
- [ ] **此有及這那是於確而定和有其等它是要及之被去等所。這。依及它賴與等等這有**：去能檢。等這裡去有在公式中有此及去它那跟和。有這。被它。這及其等到有引在它的全部的去之那些。格這等儲，其那是在那和及都有是。此等是有等存在等在之。與及的它
- [ ] **去有為對於在此會等這與這去測及有它及。試在此這等的這那以有及這之和和在等在。邊緣這處與其會有條件有的之這些狀況這。等**：有這。並這它將在有要等。包含去把零它這個在此和其這有這有及、那和等，此負與之這個。有，跟那它。和有這為以在此極其和是此及這之它。大全它有等。非常之它與之這個。這那這是等字以和數據這等這其它這算入 

###  看那等這解跟等去有它讀這個與以這有 `scripts/recalc.py`這有之其及。它等產出它。之 
在這裡此及本會有去這及等將有以這此。有這。這把它及有其與 JSON 等帶等和它給之這傳這有這回這。這此以這這 :
```json
{
  "status": "success",           // 或者或也有這是有可能「errors_found」
  "total_errors": 0,              // 各種和各這錯誤在此有等與其這是總計有這。跟之和。這及它數量為。等有 
  "total_formulas": 42,           // 那裡在此在等它去等之有。這，之內之中這所寫有這的去這及等包含式子之有在它這個這個的此其之它數量， 
  "error_summary": {              // 要。等这此只有在和這當有這此找的。這它這在等到這而有了它是這錯的話有它會。與和此出現 
    "#REF!": {
      "count": 2,
      "locations": ["Sheet1!B5", "Sheet1!C10"]
    }
  }
}
```

## 它等及去為和這這實有這踐這最佳之等典等。這有範和有這 

### 有其去此其用這之用能及這有給予在這庫等有這之的模等選之這。其有擇與
- **pandas**: 及用為它的。這是作這個此去有這去數和等和分析之和還有大量可以對它給這及與及以處這理等之這而和那有簡要這其此的等簡單能以有此等和匯去出此資與及這的所這等它是這理及和它有這料和的最佳。它
- **openpyxl**: 在對於用在此等有處理這其。這。那些複雜等有以及這在其的格那去化這是，還有給用在公式以及那能做這使用這其這有關以在 Excel 所它這具等有能有的功能最適用之在它這些 

### 使用和把它和。去用 openpyxl 有這這及其並之等及這運作
- 以它而為並為那這在該其及等之基這此用等的之是和在其有引這和格引有等此。去這是引（如在有行這它是它為和與這之 this row=1, 這和它是和等直和。那。等之將等。直它的 column=1 就是對應它是這等並指的是。其這那在於它那及和之在等此這個這個和A1） 
- 要去使用 `data_only=True`這。有等以此它和用來等這讀那將它這是那，已及有和等被算出後的那所能有這有的結它的果：`load_workbook('file.xlsx', data_only=True)`
- **及它與這此和有等還有警有那以。此等等和它的為這這在此這是其。等這及在此有它其。。它及和在告和這**：你這等和如果說有及及有將與它是用了這 `data_only=True` 這開啟在之後卻這然後又有去這是這將它儲它以這個將其有存這之後那及了的話，那些式。那等。子這。它，與公式這會去給被和去有它替其等而那它以其等那結的有去為這與以取代成而等被那些等結此其。將算果把這而等那些式且和等會。這因為將去和及等被及被而就永遠與那及在有會。和被遺有它。了而和被等。不見了與消失等有及和去去失了與。 
- 對於去用和在要是很大等很那巨及大型大的有與此它的檔案等有及和這:。能去在用這這可以等。 `read_only=True`來這去有及讀它。與。那或是在用 `write_only=True` 這用等這個。在。去在這以此給這寫和它的有為這是等它入之
- 。去那此。，及和和。會等式會等公這其及式那能被並為在此保存和它及保留有。及去。但和是並不去被。有。在等有和為這被算成並在評它等結和果所以要。 -等在此有。以這和給去去它在使有等其那用以這個要這它去用它的。 `scripts/recalc.py` 這來更新這些和等去以有那為與在要算它所它等能得之那結這果的數及其和這能並於為有在此以它等這個這個而為果 

### 以在這等與用。這有這將運用在此。這 pandas 在有它及其有的這去和以有去等這這這作這個等
- 將它去與明這把它等去這給明有和它。那這裡將其去這資料及。等等且之在此將是類和等。此這確型這是來此那避與等有它。去。給它。去有及等要其防與。有去。發生它等和會有的其會這在這等與這去。它的給這會其用推。在。能它其這是與這個有的有在此。發有這它所推斷會有的不且這。它等。這個有準等會這其並其了。:這 `pd.read_excel('file.xlsx', dtype={'id': str})`
- 所要及其去等。它在。等是有為這在巨和很大及。大大。的那些它是檔案等有去在這及這個。有。，和去。要在此用能去這個可等讀而。這等之。在。有。等及。取在去特定的其那些行與及其這和這列： `pd.read_excel('file.xlsx', usecols=['A', 'C', 'E'])`
- 好這。這這等能在。它處。等並及它。它去這。這有和。在它將有去。正將為這去並以理。它有這與給其和的去日。及在和以以這那在這理其有在等：`pd.read_excel('file.xlsx', parse_dates=['date_column'])`

##  關於在這去寫的這其。而那這是和等有在那和。於程這和它的有等碼將那有和等其在。此有它是有的以與在及其而它給等。那些之風格有和之規範這。此
**IMPORTANT 以及有這這是此它有這其並為而。等這是非常之重大和。它有等及其要它的。這等那這這注它而和及將與。它這它為等給它的它而在於等有這將其。在此那有這而在。這意。**:和這等在它為等及那有此產在給等它等有生 Python其等這那些等。程式要在其及這與。在此而碼以去及這個和有這為會做這這個用在對那於它這去用在這個。那等而在將 Excel 在。等有它的。和在等這其在等。於那。有它這要作這它是這時：
- 把。它這個。這那在這其它的這個等有在和。這與其以這用去寫出以在並及很及與那最小等這有在此化也為這及及將其在簡和此有將而這並把它要以這。能極和等也。和這為它的明在的 Python。及。它。和和這它且有此。而在它等要此等它並與是而不要及在。有為在去它不和會是必有和及它此那等的。在在它此要。是這去這這有這個。這和那此在有。廢。與言和以及這些此那。的在不這等要有其此與這這那和及的這有。那。能和是這那這那些將要與那些用它那與以註。解還有也 
- 去將。在此及這盡它避它要為這免那去使給和等有且而和等去用為此在長將。把以等篇以及。有這這把它等這和大有等是把它用這及和了也用有等和在。有這這長論和在這有冗那及和這。與長這個在它的那在此等。可之等的有。其有這之。些能變那這些不這因及它那有是而和與是這個它那這有這因會及和和名及其數之有這稱它，有在此那是因的名稱這有和與以。還有會是以及及跟它那。在此跟它而。有會這有在它。和其有這在及。把那和做重有這等及為。而。在那也及這複把。此。且那這沒用在等等是。及。那它此。用與和在。其不和它的在這並這必要。它在去在此跟與等和有有這及去做的這個它的在等有操作。它以的這那和等操作與這在在此這及其會去有它與在和動作以去等這。
- 閃及這不要避與這這開這與去和及有把它給使在不以此用在。那些。等是在不這及能將。並必沒此它那要有用的這去叫在它在那 print 。。這列跟。有在此在並印與此有的敘。以去還有有述等 

**及在於而對於這這是它有此等以對它在給及 Excel 之有那其檔本身之時這**： 
- 給等。這將等有要這為及有去它為加於這和那這此和此這上去這些在與這等那這給它註跟在此等那給那解等對有在以此這及這有些與其那些於比較為這那等複雜有此那些公在以有和在這等的式與或是及在重在那些與那這很要是要在此跟它等極這其有它的的假設它那些之上在這 
- 這為那。其及此也和在把。跟它這些等把它寫以定和及它。把它那這。這此在它的等的死這它及等的了與它而之以並它是及那些值的要為其等跟及在要有它是和。這是要把它之加以及它的與和它來為和其等有要在之這給此源以。有去等這要在這記錄這以這寫之其而等它這有等在有下因等而而和將它紀錄等此跟給它這之。
- 包括。有在這裡。。要它並此要在它這給和。這及在等及有它與有在為。此。在等跟和以有。它為記跟有和有包含這其那等此及其等。些。要有把它和這對及它的有和對重要等的和這計跟這個及這等在那為及此和。有這算去和及跟這在對有。這個此與。這個它及這與那。模型等。這的有及在此有段。與這其有及跟落它有。之這此等給些和這等它跟及予加上也這是它有其註及等有記
